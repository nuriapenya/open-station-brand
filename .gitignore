# Build output from mockups/build-landing-bundle.sh. Generated, never committed:
# GitHub Pages serves this repo from the root, so anything committed here ships.
build/

# The docs site's dependencies and its own build output, which
# build-landing-bundle.sh copies into build/landing-bundle/docs/.
node_modules/
docs-app/dist/

.DS_Store
.spacefast/state.json
/.spacefast-*
.spacefast/.spacefast-*
.spacefast/.space-link-*.tmp

# Astro local development and static output.
.astro/
.astro-public/
dist/
test-results/
playwright-report/
/assets/studio/components.js
